"""Schema-specific fixtures.

The `binary_artifact_dict` and `binary_artifact` fixtures live in the
top-level `tests/conftest.py` so storage, extractor, and API tests can
reuse them. This file exists as a deliberate empty marker so future
schema-only fixtures have an obvious home.
"""
