"""Extractor-specific fixtures.

`binary_booster_file` and `regression_booster_file` live in the
top-level `tests/conftest.py` so CLI and end-to-end tests can reuse
the same trained boosters. This file is a deliberate empty marker.
"""
